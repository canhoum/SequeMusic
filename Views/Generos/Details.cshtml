@model SequeMusic.Models.Genero

@{
    ViewData["Title"] = "Detalhes do Género";
}

<style>
    body {
        background-color: #111827;
    }

    .genero-details-card {
        background-color: rgba(255, 255, 255, 0.95);
        backdrop-filter: blur(4px);
        border-radius: 20px;
        padding: 40px;
        margin-top: 50px;
        box-shadow: 0 5px 20px rgba(0, 0, 0, 0.2);
    }

    h1, h4, dt {
        color: #1f2937;
        font-weight: 700;
    }

    dd {
        color: #374151;
    }
</style>

<div class="container d-flex justify-content-center">
    <div class="col-md-8 genero-details-card">
        <h1 class="text-center mb-4">🎧 Detalhes do Género</h1>

        <div class="text-center mb-4">
            <h4 class="fw-bold">@Model.Nome</h4>
        </div>

        <dl class="row">
            <dt class="col-sm-3">Descrição</dt>
            <dd class="col-sm-9">@Model.Descricao</dd>
        </dl>

        <div class="d-flex justify-content-between mt-4">
            <a asp-action="Index" class="btn btn-outline-secondary px-4">← Voltar</a>
            <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-primary px-4">✏️ Editar</a>
        </div>
    </div>
</div>

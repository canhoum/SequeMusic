@model SequeMusic.Models.Genero

@{
    ViewData["Title"] = "Detalhes do Género";
}

<style>
    body {
        background-color: #111827;
    }

    .genero-details-card {
        background-color: rgba(255, 255, 255, 0.95);
        backdrop-filter: blur(4px);
        border-radius: 20px;
        padding: 40px;
        margin-top: 50px;
        box-shadow: 0 5px 20px rgba(0, 0, 0, 0.2);
    }

    h1, h4, dt {
        color: #1f2937;
        font-weight: 700;
    }

    dd {
        color: #374151;
    }
</style>

<div class="container d-flex justify-content-center">
    <div class="col-md-8 genero-details-card">
        <h1 class="text-center mb-4">🎧 Detalhes do Género</h1>

        <div class="container mt-4">
            <h2 class="mb-3">@Model.Nome</h2>
            <p class="text-muted">@Model.Descricao</p>

            <h3 class="mt-5 mb-3">Músicas neste género</h3>

            @if (Model.Musicas != null && Model.Musicas.Any())
            {
                <div class="row">
                    @foreach (var musica in Model.Musicas)
                    {
                        <div class="col-md-4 mb-4">
                            <div class="card h-100 shadow-sm">
                                <div class="card-body d-flex flex-column">
                                    <h5 class="card-title">@musica.Titulo</h5>
                                    <p class="card-text text-muted">
                                        <strong>Artista:</strong> @musica.Artista?.Nome_Artista<br />
                                        <strong>Ano:</strong> @musica.AnoDeLancamento
                                    </p>
                                    <a href="@Url.Action("Details", "Musicas", new { id = musica.ID })" class="btn btn-primary mt-auto">
                                        Ver detalhes
                                    </a>
                                </div>
                            </div>
                        </div>
                    }
                </div>
            }
            else
            {
                <p class="text-secondary">Não existem músicas associadas a este género.</p>
            }
        </div>
        </div>
</div>
